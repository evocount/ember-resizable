{{#each directions as |dir|}}
  <div
    class={{concat "resizer " dir}}
    {{on "mousedown" (action this._onResizeStart dir)}}
    {{on "touchstart" (action this._onResizeStart dir)}}
  >
  </div>
{{/each}}

{{yield}}
